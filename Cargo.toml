[workspace]
members  = ["crates/*"]
resolver = "3"

[workspace.package]
authors      = ["Reverier-Xu <reverier.xu@woooo.tech>"]
description  = "IDE-style terminal emulator built on woocraft and GPUI."
edition      = "2024"
homepage     = "https://recoil.woooo.tech"
license      = "GPL-2.0-or-later"
readme       = "README.md"
repository   = "https://github.com/Reverier-Xu/recoil"
rust-version = "1.96"
version      = "0.0.1"

# Everything GUI-related must enter through the `woocraft` re-exports so the
# workspace resolves the exact upstream revisions of gpui and
# woocraft-terminal. Never add direct git dependencies on gpui, zed, or
# alacritty; see AGENTS.md and docs/roadmap.md architecture rules.
[workspace.dependencies]
recoil-core = { path = "crates/recoil-core" }
woocraft = { git = "https://github.com/Reverier-Xu/woocraft", rev = "0135e39983a39a1b3450fcaa2ce628ef28a4a0a8", default-features = false, features = [
  "terminal",
] }
woocraft-terminal = { git = "https://github.com/Reverier-Xu/woocraft", rev = "0135e39983a39a1b3450fcaa2ce628ef28a4a0a8", package = "woocraft-terminal" }
# gpui and its platform bootstrap, pinned to the exact zed revision woocraft
# itself uses so cargo unifies the graph. A direct pin is required because
# gpui proc macros emit `::gpui::` absolute paths.
gpui = { git = "https://github.com/zed-industries/zed", rev = "399258feeaf90ad8a3a208c99221ee87b6452f38", package = "gpui" }
gpui_platform = { git = "https://github.com/zed-industries/zed", rev = "399258feeaf90ad8a3a208c99221ee87b6452f38", package = "gpui_platform", features = [
  "font-kit",
  "wayland",
  "x11",
] }

anyhow = "1.0"
async-channel = "2"
chrono = { version = "0.4", default-features = false, features = [
  "clock",
  "serde",
  "std",
] }
# Already pinned in the lock graph through gpui_shared_string; the config
# JSON Schema export adds no new packages.
rust-i18n          = "4"
schemars           = "1"
serde              = { version = "1.0", features = ["derive"] }
serde_json         = "1.0"
thiserror          = "2.0"
toml               = "1.1"
tracing            = { version = "0.1", features = ["attributes"] }
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
ulid               = { version = "1.2", features = ["serde"] }

[workspace.lints.rust]
unsafe_code = "forbid"

[workspace.lints.clippy]
dbg_macro    = "deny"
print_stderr = "deny"
print_stdout = "deny"
todo         = "deny"

[profile.release]
codegen-units = 1
lto           = "thin"
opt-level     = 3
strip         = true
