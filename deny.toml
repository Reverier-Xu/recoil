[graph]
all-features = true

[advisories]
# `unmaintained` is scoped to workspace crates: the pinned gpui/woocraft
# graph transitively depends on unmaintained crates (ttf-parser, rustybuzz,
# paste) that only an upstream bump can replace. Re-evaluate on every
# woocraft rev bump.
ignore       = []
unmaintained = "workspace"
unsound      = "all"
yanked       = "deny"

[licenses]
allow = [
  "0BSD",
  "Apache-2.0",
  "BSD-2-Clause",
  "BSD-3-Clause",
  "bzip2-1.0.6",
  "ISC",
  "MIT",
  "MPL-2.0",
  "CC0-1.0",
  "Unicode-3.0",
  "Zlib",
]
confidence-threshold = 0.8
include-dev = true

[licenses.private]
ignore = true

[bans]
allow-wildcard-paths          = true
highlight                     = "all"
multiple-versions             = "deny"
multiple-versions-include-dev = true
wildcards                     = "deny"
# Duplicate-version skips are forced by the pinned gpui/woocraft graph.
# Each entry carries the same standing reason; re-evaluate the whole list on
# every dependency update and remove entries that stop being necessary.
skip = [
  { crate = "accesskit_consumer@0.36.0", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "accesskit_consumer@0.37.0", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "bitflags@1.3.2", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "block-buffer@0.10.4", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "block2@0.5.1", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "cocoa@0.25.0", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "cocoa-foundation@0.1.2", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "core-foundation@0.9.4", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "core-graphics@0.23.2", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "core-graphics-types@0.1.3", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "cpufeatures@0.2.17", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "crypto-common@0.1.7", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "digest@0.10.7", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "foldhash@0.1.5", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "font-types@0.11.3", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "getrandom@0.2.17", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "getrandom@0.3.4", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "hashbrown@0.14.5", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "hashbrown@0.15.5", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "hashbrown@0.16.1", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "heck@0.4.1", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "itertools@0.11.0", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "jni-sys@0.3.1", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "miniz_oxide@0.8.9", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "nom@7.1.3", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "objc2@0.5.2", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "objc2-app-kit@0.2.2", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "objc2-core-data@0.2.2", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "objc2-core-image@0.2.2", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "objc2-foundation@0.2.2", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "objc2-metal@0.2.2", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "objc2-quartz-core@0.2.2", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "png@0.17.16", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "pollster@0.2.5", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "r-efi@5.3.0", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "rand@0.8.8", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "rand_chacha@0.3.1", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "rand_core@0.6.4", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "read-fonts@0.37.0", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "roxmltree@0.20.0", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "rustc-hash@1.1.0", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "serde_spanned@0.6.9", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "sha2@0.10.9", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "shlex@1.3.0", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "skrifa@0.40.0", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "spin@0.9.9", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "syn@2.0.119", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "thiserror@1.0.69", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "thiserror-impl@1.0.69", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "toml@0.8.23", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "toml_datetime@0.6.11", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "toml_edit@0.22.27", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "windows@0.57.0", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "windows@0.61.3", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "windows-collections@0.2.0", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "windows-core@0.57.0", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "windows-core@0.61.2", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "windows-future@0.2.1", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "windows-implement@0.57.0", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "windows-interface@0.57.0", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "windows-link@0.1.3", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "windows-numerics@0.2.0", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "windows-result@0.1.2", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "windows-result@0.3.4", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "windows-strings@0.4.2", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "windows-sys@0.59.0", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "windows-threading@0.1.0", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
  { crate = "winnow@0.7.15", reason = "forced by the pinned gpui/woocraft graph; re-evaluate on dependency updates" },
]

[sources]
allow-git = [
  "https://github.com/Reverier-Xu/woocraft",
  "https://github.com/zed-industries/zed",
  "https://github.com/zed-industries/alacritty",
  "https://github.com/zed-industries/font-kit",
  "https://github.com/zed-industries/scap",
  "https://github.com/zed-industries/wasm_thread",
  "https://github.com/zed-industries/xim-rs.git",
]
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
unknown-git = "deny"
unknown-registry = "deny"
